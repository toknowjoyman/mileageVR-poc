<h1>Plant Based Meals</h1>
<div>
<table>
  <tr>
    <th>Date</th>
    <th>Meal</th>
    <th></th>
  </tr>

  <% @microposts.each do |meal| %>

  <tr>
    <td><%= meal.created_at.strftime("%b %d, %H:%M ")%></td>
    <td><%= link_to meal.content, meal_path(meal) %></td>
  </tr>

  <% end %>
</table>
</div>
<p> Successful Meals: <%= @count %> </p>
<%= link_to 'Home', controller: 'home_screen' %>
